class User:

    def __init__(self, access_token, athlete_id, first, last, password):
        self.access_token = access_token
        self.athlete_id = athlete_id
        self.first = first
        self.last = last
        self.password = password
